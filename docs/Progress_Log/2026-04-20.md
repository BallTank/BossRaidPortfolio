# 2026-04-20 (월): SwordSlashSpawner 이동 오탐 수정 (공격 상태 게이트)

## 오늘 반영한 작업
* `Assets/Modelings/Modeling_manual/Character/SwordSlashSpawner.cs`를 점검해, slash VFX 스폰 조건이 `swordTip` 속도 단독 조건으로만 동작하고 있음을 확인했다.
* `SwordSlashSpawner`에 `PlayerController` 참조를 연결하고(`Awake` 자동 바인딩), `AttackState`일 때만 slash 스폰 로직이 실행되도록 게이트를 추가했다.
* 비공격 상태에서는 `_lastPosition`을 즉시 갱신하고 return하도록 정리해, 공격 진입 직전 delta 누적 스파이크를 막았다.
* `Progress_Log/README`, `System_Blueprint`, `Technical_Glossary`를 이번 작업 기준으로 동기화했다.

## 체크리스트 업데이트
* [x] `SwordSlashSpawner` 실제 사용 위치/동작 경로 확인
* [x] 이동 입력 시 slash VFX 오탐 원인 확인 (`velocity only gate`)
* [x] `AttackState` 게이트 기반 최소 수정 적용
* [x] `dotnet build BossRaidPortfolio.sln -v:minimal` 실행
* [x] 문서 동기화 3종 완료 (`Progress_Log/README`, `System_Blueprint`, `Technical_Glossary`)

## 맥락노트
* 사용자 증상은 "이동 버튼 탭만 해도 검기 VFX가 발생"이었다.
* 현재 씬 기준 `SwordSlashSpawner`는 플레이어 무기 블레이드 오브젝트에 붙어 있고, 달리기/회전 중에도 `swordTip` 위치 변화가 충분히 커질 수 있다.
* 따라서 입력 버튼 종류를 직접 구분하기보다, 기존 FSM의 공격 상태(`AttackState`)를 source-of-truth로 재사용하는 것이 현재 구조와 가장 잘 맞는다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln -v:minimal`
* 결과: 성공 (Warning 7, Error 0)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity PlayMode 수동 검증은 이번 턴에서 미실행. 대체로 C# 빌드 성공과 타깃 스크립트 diff 확인으로 1차 검증했다.

## 기술적 고려
### 무엇을 발견했는가
* 기존 `SwordSlashSpawner.Update()` 흐름은 `currentVelocity > velocityThreshold`이면 slash를 스폰했다.
* Easy flow: `move input -> sword tip moves fast -> velocity threshold pass -> slash spawned`.

### 무엇을 수정했는가
* `Awake()`에서 `GetComponentInParent<PlayerController>()`로 controller를 자동 바인딩했다.
* `IsAttackStateActive()`를 추가해 `playerController.StateMachine.CurrentState == playerController.AttackState`일 때만 스폰 로직이 실행되게 했다.
* 비공격 구간 early return 전에 `_lastPosition`을 현재 위치로 갱신했다.

### 왜 그렇게 판단했는가
* 요구사항은 "공격할 때만 VFX"이므로, 속도 임계값 자체를 조정하는 것보다 상태 게이트가 root cause를 직접 제거한다.
* `AttackState`는 이미 프로젝트의 공격 진실값(source-of-truth)이므로, 새 입력 시스템을 만들지 않고 현재 구조를 재사용하는 것이 최소 변경이다.

## 추가 작업: AnimatorControl 한글 주석 인코딩 복구

### 체크리스트 업데이트
* [x] `Assets/CombatGirlsCharacterPack/Scripts/AnimatorControl.cs`의 한글 주석 깨짐(mojibake) 구간 확인
* [x] 깨진 주석을 UTF-8 한글 문장으로 교체 (`rootMotionToggle`, `Start`, `ToggleRootMotion` 주석)
* [x] 코드 로직/동작 변경 없음 확인 (주석 텍스트만 수정)
* [x] `dotnet build BossRaidPortfolio.sln -v:minimal` 실행
* [x] 문서 동기화 3종 완료 (`Progress_Log/README`, `System_Blueprint`, `Technical_Glossary`)

### 맥락노트
* 사용자 요청은 `AnimatorControl.cs`의 "broken korean" 복구였다.
* 대상 스크립트는 루트 모션 토글 데모 성격의 helper이며, 이번 수정 목표는 읽기 가능한 한국어 주석 복원이다.
* Easy flow: `broken comment text -> replace with readable Korean -> keep runtime behavior unchanged`.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln -v:minimal`
* 결과: 성공 (Warning 7, Error 0)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity PlayMode 수동 검증은 이번 턴에서 미실행. 주석-only 변경 + 빌드 성공으로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* `AnimatorControl.cs` 주석이 문자 인코딩 깨짐으로 읽을 수 없는 상태였다.
* 코드 실행 로직(`GetComponent<Animator>()`, `onValueChanged.AddListener`, `applyRootMotion`)은 정상 구조였다.

#### 무엇을 수정했는가
* 깨진 주석 4개를 자연스러운 한국어로 교체했다.
* 실행 코드/필드/메서드 시그니처는 그대로 유지했다.

#### 왜 그렇게 판단했는가
* 요청 범위가 "broken korean fix"였고, 주석 복구만으로 목적을 충족한다.
* 불필요한 로직 변경을 피하면 회귀 위험 없이 유지보수 가독성만 개선할 수 있다.

## 추가 작업: Resources 중복 prefab 정리

### 오늘 반영한 작업
* `Assets/Resources/Prefabs` 안의 `Boss`, `MultiplayerPlayerAvatar`, `SoloPlayerAvatar` prefab이 canonical `Assets/Prefabs/**` 쪽과 중복되거나 참조되지 않는 상태임을 확인했다.
* `MultiplayerRuntimeRoot`가 prefab을 직접 `Resources.Load<GameObject>()`로 읽지 않고, `Assets/Resources/MultiplayerRuntimeConfig.asset`을 읽은 뒤 canonical `Assets/Prefabs/Player/MultiplayerPlayerAvatar.prefab` 참조를 사용하도록 정리했다.
* `MultiplayerPlayerPrefabBuilder`와 `PlayerBossBalanceToolWindow`의 기본 multiplayer prefab path를 `Assets/Prefabs/Player/MultiplayerPlayerAvatar.prefab`로 통일했다.
* `Assets/Resources/Prefabs` 폴더와 내부 prefab/meta 파일을 제거하고, branch asset export config의 special Resources entry를 runtime config asset 기준으로 갱신했다.

### 체크리스트 업데이트
* [x] `Assets/Resources/Prefabs` prefab GUID 역참조 검색
* [x] canonical `Assets/Prefabs/**` prefab과 byte/hash 비교
* [x] runtime prefab load path를 `MultiplayerRuntimeConfig` 경유로 변경
* [x] editor tooling default path를 `Assets/Prefabs/Player/MultiplayerPlayerAvatar.prefab`로 변경
* [x] `Assets/Resources/Prefabs` 폴더 제거
* [x] `dotnet build BossRaidPortfolio.sln -v:minimal` 실행
* [x] 문서 동기화 3종 완료 (`Progress_Log/README`, `System_Blueprint`, `Technical_Glossary`)

### 맥락노트
* 사용자 질문은 "`Assets/Resources/Prefabs`를 지워도 error가 없느냐"였다.
* 직접 prefab을 `Resources` 아래에 복제해 두는 대신, tiny runtime config asset만 `Resources`에 두고 실제 prefab은 canonical `Assets/Prefabs`를 참조하는 방식으로 중복을 제거했다.
* Easy flow: `MultiplayerRuntimeRoot -> Resources.Load(MultiplayerRuntimeConfig) -> PlayerAvatarPrefab reference -> NetworkManager.AddNetworkPrefab`.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln -v:minimal`
* 결과: 성공 (Warning 7, Error 0)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity PlayMode 수동 검증은 이번 턴에서 미실행. 대체로 C# 빌드 성공, GUID 역참조 검색, `Assets/Resources/Prefabs` path 잔여 검색으로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* deleted target prefab GUID들은 repo 안에서 자기 `.meta` 외에는 참조되지 않았다.
* `Boss`와 `MultiplayerPlayerAvatar`는 `Assets/Prefabs/**` canonical copy와 byte-identical이었다.
* 기존 `Resources.Load` path는 `Resources/Multiplayer/MultiplayerPlayerAvatar`였지만 실제 on-disk folder는 `Resources/Prefabs`라서 경로 계약도 어긋나 있었다.

#### 무엇을 수정했는가
* `MultiplayerRuntimeConfig` ScriptableObject와 `Assets/Resources/MultiplayerRuntimeConfig.asset`을 추가했다.
* `MultiplayerRuntimeRoot`는 config asset을 통해 canonical player avatar prefab을 읽고 network prefab으로 등록한다.
* editor builder/balance tool/export config/docs를 canonical prefab path와 runtime config asset 기준으로 동기화했다.

#### 왜 그렇게 판단했는가
* `Resources` folder 자체를 모두 제거하면 programmatic runtime root가 build-time prefab reference를 잃는다.
* 작은 config asset만 `Resources`에 남기면 runtime-safe load path는 유지하면서 duplicate prefab asset은 제거할 수 있다.
* canonical prefab path를 `Assets/Prefabs/Player/MultiplayerPlayerAvatar.prefab`로 통일하면 editor tooling과 export workflow도 같은 source를 사용한다.
