# 2026-04-30 (목): Boss attack warning 공용 visual height offset 추가

## 오늘 반영한 작업
* `Assets/Scripts/Boss/BossController.cs`의 메인 `Attack Settings` 맨 아래에 `warningVisualHeightOffset` inspector field를 추가해 Attack1/2/4 warning visual이 같은 Y lift 값을 읽도록 정리했다.
* 기존 AoE `groundOffset`는 runtime ground projection용 hidden field로 내리고, attack warning 높이 조절 용도로는 더 이상 inspector main path에서 사용하지 않도록 분리했다.
* `Assets/Scripts/Boss/AttackWarningController.cs`에 visual-only height offset 경로를 추가해 warning visual child만 위로 올리고, damage query center(`transform.position`)는 그대로 유지하도록 정리했다.
* `Assets/Scripts/Boss/AoE/AoECircleController.cs`, `Assets/Scripts/Boss/Attacks/AoEAttackPattern.cs`를 따라 shared offset이 pooled AoE circle, Basic/Lunge cloned telegraph, replicated display warning까지 같은 값으로 적용되게 연결했다.
* `docs/Progress_Log/README.md`, `docs/technical/System_Blueprint.md`, `docs/technical/Technical_Glossary.md`를 이번 변경 기준으로 동기화했다.

## 체크리스트 업데이트
* [x] `BossController` Attack Settings bottom에 공용 warning visual height field 추가
* [x] Attack1/2/4 warning visual-only Y lift 경로 구현
* [x] AoE 기존 `groundOffset`를 hidden runtime projection field로 유지
* [x] pooled / replicated warning path에 same offset 적용 확인
* [x] `dotnet build BossRaidPortfolio.sln` 실행
* [x] 문서 동기화 3종 완료 (`Progress_Log/README`, `System_Blueprint`, `Technical_Glossary`)

## 맥락노트
* 사용자 목표는 uneven terrain에서도 boss attack red area를 더 잘 보이게 하되, 실제 hit logic은 바꾸지 않는 것이었다.
* current code는 warning object root position이 damage overlap query center로도 재사용되어, root Y를 직접 올리면 visual-only tweak가 hit mismatch로 이어질 수 있었다.
* 그래서 이번 작업은 `ground projection`과 `visual lift`를 분리하고, inspector에서는 one master field만 노출하는 방향으로 정리했다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (0 Error, warning only)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity Play Mode 수동 확인은 이번 턴에서 미실행. 대체로 build success와 warning visual path diff 검토로 1차 검증했다.

## 기술적 고려
### 무엇을 발견했는가
* `AttackWarningController`는 visual playback과 damage query를 같은 object root에서 함께 다루고 있었다.
* AoE `groundOffset`는 warning visibility knob처럼 보였지만, 실제로는 ground projection 결과 Y에 섞이는 runtime placement 값이었다.
* Basic/Lunge warning은 cloned telegraph path, AoE는 pooled circle path를 타므로, shared offset은 여러 entry point에 같이 연결해야 했다.

### 무엇을 수정했는가
* `AttackWarningController.VisualSettings`와 runtime helper에 `visualHeightOffset`을 추가하고, visual root local position만 vertical offset을 적용하도록 바꿨다.
* `AoECircleController.SetSharedWarningVisualHeightOffset(...)`, `BossController.ApplySharedWarningVisualHeightOffset(...)`, `AoEAttackPattern` pool acquire path를 추가해 shared offset을 모든 warning path에 주입했다.
* `BossController` 메인 `Attack Settings`에 `warningVisualHeightOffset`를 추가하고, 기존 AoE `groundOffset`는 `[HideInInspector]`로 내려 inspector main path에서 분리했다.

### 왜 그렇게 판단했는가
* visual readability 문제를 terrain flatten보다 smaller scope에서 해결하려면 warning mesh/root child만 올리는 편이 안전하다.
* shared inspector field를 `BossController`에 두면 designer가 Attack1/2/4를 one place에서 빠르게 맞출 수 있다.
* damage center를 유지한 채 visual만 올리면 slope/terrain variation 대응과 hit truth 보존을 동시에 달성할 수 있다.

---

## Topic 2. Basic/Lunge warning invisible fix (particle renderer -> fallback mesh)

### 오늘 반영한 작업
* `Assets/Scripts/Boss/AttackWarningController.cs`의 renderer capability check를 보강해 `ParticleSystemRenderer`를 warning visual renderer로 더 이상 허용하지 않도록 수정했다.
* boss `CirclePrefab`처럼 particle effect child만 가진 warning source에서는 runtime fallback disc/strip mesh가 자동으로 생성되도록 유도했다.
* `docs/technical/System_Blueprint.md`, `docs/technical/Technical_Glossary.md`에 same fallback rule을 반영했다.

### 체크리스트 업데이트
* [x] `AttackWarningController` particle renderer fallback guard 추가
* [x] `dotnet build BossRaidPortfolio.sln` 재실행
* [x] 문서 동기화 3종 유지 (`Progress_Log`, `System_Blueprint`, `Technical_Glossary`)

### 맥락노트
* 사용자 증상은 `Basic Attack` warning red area가 보이지 않는 것이었다.
* 현재 boss warning prefab은 explicit `warningRenderer`가 비어 있고, child가 mostly particle renderer 기반이라 auto-bind가 wrong renderer를 잡을 수 있었다.
* 따라서 particle renderer를 valid warning surface로 취급하지 않고 fallback mesh를 쓰게 하는 편이 가장 작은 수정 범위였다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (0 Error, warning only)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity Play Mode 실기동은 이번 턴에서 미실행. 대체로 compile success와 prefab wiring inspection으로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* `AttackWarningController.InitializeVisualRuntime()`는 explicit binding이 없을 때 `GetComponentInChildren<Renderer>(true)`로 first renderer를 auto-pick한다.
* boss `CirclePrefab`에서는 이 auto-pick 대상이 particle effect renderer가 되어 sector/strip warning visual로 적합하지 않았다.

#### 무엇을 수정했는가
* `TryConfigureRendererCapabilities()`에서 `ParticleSystemRenderer`를 invalid renderer로 분류하고 `false`를 반환하도록 변경했다.
* same branch에서 existing runtime fallback mesh 생성 경로가 실행되어 Basic/Lunge warning이 simple red mesh로 재생되게 했다.

#### 왜 그렇게 판단했는가
* prefab 교체나 scene rewiring보다 capability gate 하나를 좁히는 편이 더 안전하고 빠르다.
* runtime fallback mesh는 이미 current codebase가 지원하는 경로라, new visual system을 추가하지 않고도 visibility 회귀를 복구할 수 있다.

---

## Topic 3. Client Basic attack/warning replay timing cushion

### 오늘 반영한 작업
* Assets/Scripts/Multiplayer/Gameplay/MultiplayerBossAuthorityBridge.cs에서 remote client replay 순서를 조정해 replicated warning/effect를 authoritative boss state보다 먼저 적용하도록 바꿨다.
* same bridge에 Basic Attack 전용 small presentation grace(.08s)를 추가해 warning이 아직 안 켜진 frame에서는 client attack animation normalized time을 약간 뒤로 당기도록 보정했다.
* Assets/Scripts/Boss/BossController.cs에 current basic warning running state read surface를 추가해 bridge가 local warning playback 상태를 안전하게 확인할 수 있게 했다.
* docs/Progress_Log/README.md, docs/technical/System_Blueprint.md, docs/technical/Technical_Glossary.md를 이번 multiplayer timing follow-up 기준으로 갱신했다.

### 체크리스트 업데이트
* [x] client boss replay에서 effect-before-state 적용
* [x] Basic Attack client-only small timing cushion 추가
* [x] dotnet build BossRaidPortfolio.sln 재실행
* [x] 문서 동기화 3종 유지 (Progress_Log, System_Blueprint, Technical_Glossary)

### 맥락노트
* 사용자 증상은 client 화면에서 Basic Attack damage/animation이 warning보다 빨라 보이는 것이었다.
* current multiplayer boss replay는 attack animation을 authoritative state stream에서, warning visual을 replicated effect stream에서 따로 받는다.
* 그래서 exact lockstep보다 warning first + tiny attack visual delay가 current architecture에서 더 작은 수정 범위였다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: dotnet build BossRaidPortfolio.sln
* 결과: 성공 (0 Error, warning only)
* 핵심 경고: 기존 MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict), CS0414, CS0649
* 미실행 사유 및 대체 검증: Unity Play Mode 실기동은 이번 턴에서 미실행. 대체로 compile success와 replay timing path diff 검토로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* client MultiplayerBossAuthorityBridge.Update()는 기존에 latest boss state를 먼저 적용하고, 그 다음 frame path에서 pending warning/effect를 재생했다.
* Basic attack은 remote client에서 AttackNormalizedTime/AttackPlaybackSpeed snapshot으로 즉시 재생되므로, warning effect packet이 한 frame만 늦어도 attack이 앞서 보일 수 있었다.

#### 무엇을 수정했는가
* client update 순서를 ApplyPendingReceivedEffects() -> ApplyLatestReceivedState()로 바꿨다.
* Basic replay에 warning 미재생 시에만 적용되는 .08s normalized grace를 더해 attack visual이 warning을 앞지르지 않게 완충했다.

#### 왜 그렇게 판단했는가
* warning과 attack truth를 하나의 unified packet으로 다시 설계하는 것은 current bug scope보다 크고 회귀 위험이 높다.
* 반면 effect-first replay와 small client-only bias는 host damage truth를 건드리지 않으면서 사용자 체감 불일치를 줄이는 데 가장 직접적이다.

---

## Topic 4. Basic fallback warning source VFX suppression

### 오늘 반영한 작업
* `Assets/Scripts/Boss/AttackWarningController.cs`에 fallback red mesh 활성 시 source prefab의 particle/VFX child를 임시로 비활성화하는 경로를 추가했다.
* warning playback 종료 시에는 이번 playback에서 잠시 숨겼던 source visual child를 다시 복원하도록 정리했다.
* `docs/Progress_Log/README.md`, `docs/technical/System_Blueprint.md`, `docs/technical/Technical_Glossary.md`를 이번 follow-up 기준으로 갱신했다.

### 체크리스트 업데이트
* [x] fallback warning active 중 source particle/VFX child suppress 적용
* [x] playback 종료 시 suppressed source visual restore 적용
* [x] `dotnet build BossRaidPortfolio.sln` 재실행
* [x] 문서 동기화 3종 유지 (`Progress_Log`, `System_Blueprint`, `Technical_Glossary`)

### 맥락노트
* 사용자 보고는 client에서 attack delay는 줄었지만, red warning 대신 old circling particle effect가 다시 보인다는 것이었다.
* current boss `CirclePrefab`는 particle child가 auto-play되는 구조라, fallback red mesh만 추가해도 source VFX가 동시에 살아 있으면 wrong visual이 계속 보일 수 있었다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (0 Error, warning only)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity Play Mode 실기동은 이번 턴에서 미실행. 대체로 compile success와 fallback visual path diff 검토로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* particle renderer를 invalid warning renderer로 분류해도, prefab child GameObject 자체는 active 상태라 warning object activation 시 old circling effect가 그대로 재생될 수 있었다.
* 따라서 renderer capability gate만으로는 wrong source visual reappearance를 완전히 막지 못했다.

#### 무엇을 수정했는가
* `AttackWarningController.PrepareVisualForCurrentShape()`에서 runtime fallback을 쓰는 경로에 source particle/VFX child suppress를 추가했다.
* `FinishPlayback()`에서 same playback 동안 비활성화했던 source visual child를 다시 restore하도록 정리했다.

#### 왜 그렇게 판단했는가
* prefab 자체를 즉시 갈아끼우는 것보다 fallback runtime path에서 source visual만 잠시 숨기는 편이 수정 범위가 작고 회귀 위험이 낮다.
* 이 방식은 red warning mesh visibility를 살리면서도, later에 proper prefab rewiring이 필요하면 구조를 크게 바꾸지 않고 유지할 수 있다.

---

## Topic 5. Basic runtime telegraph fallback 정착 + first-use limitation 문서화

### 오늘 반영한 작업
* `Assets/Scripts/Boss/BossController.cs`에서 Basic warning resolve path를 분리해, direct `basicAttackWarningPrefab` override가 없으면 plain runtime object `BasicAttackWarning_Runtime`를 생성하도록 정리했다.
* 이 runtime object는 `AoECircleController`를 거치지 않고 `AttackWarningController`만 붙여서 sector fallback mesh가 particle renderer path로 다시 되감기지 않도록 고정했다.
* investigation 동안 넣었던 `BasicWarnDebug` 계열 temporary log는 `BossController`, `AttackWarningController`에서 모두 제거했다.
* current portfolio scope에서는 첫 번째 Basic replay의 one-time warm-up issue(ideal delay/pose 미정렬)가 남아 있음을 accepted limitation으로 기록하고, second replay onward stable behavior를 current keep-state로 남긴다.

### 체크리스트 업데이트
* [x] Basic 전용 runtime warning object path 정착
* [x] `AoECircleController` reset 없이 fallback sector 유지
* [x] temporary debug trace 제거
* [x] accepted first-use limitation 문서화

### 맥락노트
* 로그 확인 결과 previous Basic warning invisible bug는 “fallback mesh가 만들어진 뒤 AoE-style visual setting이 다시 덮어써지는 문제”와 연결되어 있었다.
* plain runtime `AttackWarningController` object를 따로 만들면 this reset path를 우회할 수 있고, current second-use replay에서는 visible result가 안정적으로 유지됐다.
* 다만 very first Basic replay는 object create/warm-up frame 영향으로 delay/position이 ideal하지 않을 수 있었고, 사용자와 합의한 범위에서는 portfolio limitation으로 남기기로 했다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (0 Error, warning only)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity Play Mode full regression은 이번 턴에서 미실행. 대신 runtime warning resolve path diff, temporary trace 제거 확인, build success로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* `AoECircleController` 기반 warning source는 Basic sector fallback mesh를 만든 뒤에도 visual setting을 다시 apply해 wrong renderer path로 되돌릴 수 있었다.
* 또한 first-use replay는 runtime object 생성 직후 frame의 timing/pose warm-up 영향이 남아 perfect first impression을 보장하지 못했다.

#### 무엇을 수정했는가
* Basic warning만 `CreateBasicAttackRuntimeTelegraph()` 경로를 통해 plain `AttackWarningController` object를 생성하도록 분리했다.
* investigation 완료 후 `BasicWarnDebug` temporary logs를 모두 제거해 current branch를 keep-state로 정리했다.

#### 왜 그렇게 판단했는가
* Lunge/AoE는 current path가 이미 안정적이므로, Basic만 narrow fix하는 편이 회귀 위험이 가장 낮다.
* first-use warm-up issue는 current portfolio scope에서 critical gameplay bug보다 polish issue에 가깝고, second replay onward behavior가 stable하므로 accepted limitation으로 남기는 편이 cost 대비 합리적이다.

---

## Topic 6. Multiplayer player DamageCaster runtime rebinding fix

### 오늘 반영한 작업
* `Assets/Scripts/Player/PlayerController.cs`에 active visual 기준 `DamageCaster` runtime rebind 경로를 추가했다.
* `Awake()`와 `RefreshVisualBindings()`에서 current active `PlayerVisual` 아래의 `DamageCaster`를 다시 찾고, owner/event binding을 안전하게 재연결하도록 정리했다.
* shared multiplayer player prefab이 serialized `_damageCaster`를 `null`로 유지하더라도, host/client visual role 전환 뒤 실제 active katana hitbox를 다시 잡도록 보강했다.
* `docs/Progress_Log/README.md`, `docs/technical/System_Blueprint.md`, `docs/technical/Technical_Glossary.md`를 이번 multiplayer player hitbox fix 기준으로 동기화했다.

### 체크리스트 업데이트
* [x] `PlayerController` active visual 기반 `DamageCaster` runtime rebinding 추가
* [x] old/new `DamageCaster` event subscribe/unsubscribe safe handoff 보강
* [x] `dotnet build BossRaidPortfolio.sln` 실행
* [x] 문서 동기화 3종 완료 (`Progress_Log`, `System_Blueprint`, `Technical_Glossary`)

### 맥락노트
* 사용자 증상은 multiplayer host/client player 모두 katana object는 존재하지만 `PlayerController.damageCaster` reference가 비어 있어 boss를 때리지 못하는 것이었다.
* current shared network player prefab은 owner role에 따라 host/client visual root를 runtime에서 바꾸지만, `PlayerController`는 기존에 `PlayerVisual`/`BlinkWhiteEffect`만 self-heal하고 `DamageCaster`는 serialized reference에만 의존하고 있었다.
* 그래서 multiplayer prefab의 `_damageCaster = null` contract와 runtime visual swap이 함께 있을 때 attack animation event는 살아 있어도 actual hitbox open이 비어 있는 상태가 발생했다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (0 Error, warning only)
* 핵심 경고: 기존 `MSB3277(System.Net.Http/System.Security.Cryptography.Algorithms version conflict)`, `CS0414`, `CS0649`
* 미실행 사유 및 대체 검증: Unity Play Mode 실기동은 이번 턴에서 미실행. 대체로 build success와 prefab/runtime binding path 검토로 1차 검증했다.

### 기술적 고려
#### 무엇을 발견했는가
* `Assets/Prefabs/Player/MultiPlayerAvatar.prefab`은 `_damageCaster`를 명시적으로 `null` override하고 있었고, 대신 visual hierarchy 안 blade object에는 `DamageCaster`가 남아 있었다.
* `MultiplayerPlayerAvatar.ApplyVisualRole()`는 owner role에 따라 active visual root를 바꾸고 `PlayerController.RefreshVisualBindings()`를 호출하지만, 기존 `PlayerController`는 combat hitbox binding을 갱신하지 않았다.

#### 무엇을 수정했는가
* `PlayerController.ResolveDamageCasterBinding(...)`와 old/new `DamageCaster` bind/unbind helper를 추가했다.
* current active `PlayerVisual` 아래 `DamageCaster`를 우선 선택하고, 없으면 player root child 전체를 fallback으로 다시 탐색하도록 정리했다.
* rebind 시 old hitbox를 강제로 닫고 event/owner binding을 새 caster로 다시 연결했다.

#### 왜 그렇게 판단했는가
* prefab asset에 one fixed sword reference를 넣는 방식은 host/client visual swap 이후 inactive visual의 hitbox를 계속 가리킬 위험이 있었다.
* active visual 기준 runtime rebinding은 current shared network prefab + owner-based presentation architecture를 그대로 재사용하면서 root cause를 가장 작은 범위로 복구한다.
