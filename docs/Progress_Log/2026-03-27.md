# 2026-03-27 (multiplayer Path B predicted render follow-up)

## 오늘 반영한 작업
* `clientPlayer` `PlayerController` Inspector를 단순화했다.
  * `_multiplayerPredictedRenderLateralLeadFraction`만 잠시 single visible gauge로 열어 grouped trace를 읽었다.
  * latest follow-up에서는 이 gauge도 다시 hide 처리했다.
* `[MultiplayerPredictedRenderTrace]`를 기반으로 `leadValue`와 `behindTicks`를 직접 비교했다.
* latest grouped result에서는 `0.0`이 pure `A / D` strafe에서 가장 낮은 `behindTicks`를 보여 current hidden baseline으로 채택했다.
* latest render-shape simplification에서는 lateral-only branch를 active path에서 제거하고, pure locomotion 전 방향이 같은 `previous predicted tick -> current predicted tick` interpolation을 쓰도록 정리했다.
* latest timing-shape follow-up에서는 같은 shared interpolation path를 유지하되, `alpha`를 linear에서 stronger faster-early cubic ease-out curve로 바꿔 steady strafe 동안 visible body가 tick 내내 뒤에 남는 시간을 더 줄이도록 조정했다.
* latest tick-boundary follow-up에서는 same shared interpolation / cubic ease-out path를 유지한 채, first render frame이 exact `alpha=0`에서 시작하지 않도록 small `alphaFloor = 0.05`를 추가했다.
* latest grouped visual reading correction에서는 `behindTicks`만 줄이는 방향이 항상 더 smooth한 것은 아니라고 정리했다.
  * easy English: bigger `alphaFloor` reduced lag numbers, but increased `visualVelMag` spikes and looked more jittery.
  * current calmer visual zone is around `0.04 ~ 0.07`.
* `MultiplayerPlayerAvatar` Inspector guide help box는 그대로 유지한다.

## 체크리스트 업데이트
* [x] Path B predicted render trace metrics 정리
* [x] grouped lead comparison 반영
* [x] `_multiplayerPredictedRenderLateralLeadFraction` hidden baseline을 `0.0`으로 재설정
* [x] lateral-only predicted render branch 제거
* [x] predicted render tick-boundary `alphaFloor` follow-up 추가
* [x] runtime/editor `dotnet build` 검증

## 기술 메모
### 무엇을 발견했는가
* network side는 계속 healthy했다.
* easy English: `predict` was still the main path, and the remaining pure `A / D` issue stayed render-side.
* grouped trace 기준:
  * `0.0` was best
  * `0.1` was next best
  * both negative values and bigger positive values were worse

### 무엇을 수정했는가
* `Assets/Scripts/Player/PlayerController.cs`
  * `_multiplayerPredictedRenderLateralLeadFraction` default를 `0.0f`로 바꿨다.
  * Inspector 노출은 다시 숨겼다.
  * active render path에서는 lateral-only lead branch를 제거하고, pure locomotion 전 방향이 같은 tick interpolation rule을 쓰도록 정리했다.
  * latest follow-up에서는 `linearAlpha`를 stronger `cubic ease-out interpAlpha`로 변환해 same shared interpolation path가 tick 초반에 더 빨리 current target 쪽으로 따라붙게 만들었다.
* latest narrow follow-up에서는 `EvaluatePredictedPresentationLinearAlpha(...)`를 추가해 tick-boundary frame에도 minimum start progress(`alphaFloor = 0.05`)를 적용하도록 바꿨다.
* `[MultiplayerPredictedRenderTrace]`에도 `alphaFloor` field를 추가했다.
* latest grouped visual reading에서는 `visualVelMag`를 같이 읽어 high `alphaFloor`가 body chase/popping을 키우는지 비교했다.
* 문서 동기화:
  * `docs/technical/System_Blueprint.md`
  * `docs/technical/Technical_Glossary.md`
  * `docs/technical/multiplayer/Multiplayer_Client_Movement.md`
  * `docs/Progress_Log/README.md`

### 왜 그렇게 판단했는가
* latest grouped trace에서 `0.0`이 가장 낮은 `behindTicks`를 기록했다.
* easy English: more lateral lead was not helping anymore.
* current best move is to keep one shared render interpolation rule first, not keep a separate `A / D` branch.
* latest timing-shape guess says the remaining slight jitter is more likely from `linear alpha spends too much of the tick behind`, so the next narrow follow-up changed only that alpha curve first.
* latest boundary-frame guess says the remaining one-tick-behind sample is mostly `linearAlpha=0` start frame, so the next narrow follow-up changed only the start timing of that interpolation window.
* latest correction says the final visual choice must weigh `visualVelMag` too, not just `behindTicks`.

## 코드 검증 결과
* 명령: `dotnet build Assembly-CSharp.csproj -v:minimal /nodeReuse:false`
* 결과: passed
* 명령: `dotnet build Assembly-CSharp-Editor.csproj -v:minimal /nodeReuse:false`
* 결과: first try hit temporary `VBCSCompiler` file lock, rerun passed

## 다음 확인 사항
* pure `A / D`
* `left -> forward -> right`
* forward/back가 current good state를 유지하는지
* `behindTicks`가 steady strafe에서 더 낮게 유지되는지
