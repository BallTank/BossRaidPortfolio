# 2026-04-03 (금): verify HP HUD refresh + boss client presentation mirror

## Topic 1. verify scene HP HUD refresh fallback

## 오늘 반영한 작업
* `CombatHUDController`에 HP source change polling fallback을 추가해, verify scene에서 player `Health` 값이 변하면 HUD fill도 같은 frame 범위에서 다시 따라오도록 보강했다.
* 초기 `Initialize(...)` 바인딩 시 마지막 관측 HP 캐시를 reset하고, `LateUpdate()`에서 player/boss `CurrentHealth`/`MaxHealth` 변화를 비교해 필요한 경우만 bar를 refresh하도록 정리했다.
* 이 경로는 event-driven update를 대체하는 것이 아니라, event miss 또는 bind timing edge가 있어도 verify test HUD가 full 상태로 남지 않게 하는 safety net이다.

## 체크리스트 업데이트
* [x] `GamePlayScene_Verify` solo test에서 inspector HP 변화가 HUD fill에 반영될 fallback을 추가했다.
* [x] player/boss HP fill refresh는 기존 event path를 유지하면서도 runtime polling safety net을 함께 가진다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user report 기준으로 verify scene solo path에서는 player inspector HP는 내려가는데 `Canvas/PlayerHUD_Panel/PlayerHP/Image_PlayerHP_Fill`이 full로 남아 있었다.
* current HUD 구조는 `Health.OnDamageTaken/OnDeath` 기반 즉시 갱신이 기본이지만, verify regression을 재현할 때는 `bind timing / missed refresh`도 함께 의심해야 했다.
* 이번 slice 목표는 UI prefab을 크게 갈아엎는 것이 아니라, verify test에서 HP truth가 이미 내려간 경우 HUD도 반드시 따라오게 만드는 것이다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* verify scene에서는 runtime `Health` 값이 내려가도 HUD fill이 full로 남는 case가 있었다.
* 현재 `CombatHUDController`는 event-driven update만 신뢰하고 있어, bind timing edge나 refresh miss가 있으면 visual stale state가 남을 수 있었다.
* user test 목적은 `solo HP truth -> HUD visible` 확인이므로, verify scene에서는 one more guard가 필요했다.

### 무엇을 수정했는가
* `CombatHUDController`에 마지막 관측 HP 캐시를 추가했다.
* `LateUpdate()`에서 player/boss `CurrentHealth`/`MaxHealth` 변화를 비교해 바가 stale이면 `RefreshPlayerHealthBar()` / `RefreshBossHealthBar()`를 다시 호출하게 했다.
* `Initialize(...)` 시점에는 캐시를 reset해 rebind 직후 첫 refresh가 반드시 실행되도록 맞췄다.

### 왜 그렇게 판단했는가
* verify scene bug의 핵심은 `HUD가 full로 남는다`는 visible symptom이므로, runtime health source를 직접 비교하는 fallback이 가장 빠르고 안전했다.
* event path는 유지해야 solo/main HUD contract를 깨지 않으므로, polling은 `only when changed` safety net으로만 두는 편이 맞다.
* 이 방식은 solo/multiplayer 공통 HUD를 그대로 유지하면서 verify regression만 좁게 막아 준다.

## Topic 2. boss client presentation mirror for multiplayer verify

## 오늘 반영한 작업
* `MultiplayerPlayerAvatar`가 server authority replica에서 boss presentation snapshot을 owner client로 보내도록 추가했다.
* snapshot에는 boss `position`, `rotation`, current animator `state hash`, `normalized time`, `Speed` parameter, animator playback speed를 담고, client owner는 local disabled boss에 이를 apply한다.
* `MultiplayerGameplaySceneCoordinator.DisableClientSideBossAuthority()`는 client에서 boss logic뿐 아니라 local `CharacterController`도 함께 끄도록 정리해, client boss가 AI/physics truth를 쓰지 않고 Host snapshot display만 따르도록 만들었다.

## 체크리스트 업데이트
* [x] verify multiplayer path에서 client screen이 Host boss movement를 볼 수 있도록 boss presentation mirror를 추가했다.
* [x] current slice는 full boss gameplay replication이 아니라 movement + current attack animation state mirror임을 문서에 명시했다.
* [x] client boss는 local AI/physics authority를 쓰지 않고 Host snapshot display-only path를 따른다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user report 기준 multiplayer verify blocker는 `Host에서 움직이는 boss가 client screen에는 보이지 않는다`였다.
* inspection 결과 current codebase에는 dedicated multiplayer boss actor/network object가 없고, client side boss authority는 의도적으로 꺼져 있었다.
* 그래서 이번 slice는 full boss AI netcode를 새로 만드는 대신, existing player avatar RPC path를 사용해 owner client에 boss display snapshot만 내려 보내는 minimal unblock fix로 잡았다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* verify multiplayer scene의 boss는 client에서 `BossController`가 disabled라 local movement/attack이 전혀 갱신되지 않았다.
* current codebase에는 boss 전용 `NetworkBehaviour`, `NetworkTransform`, `NetworkAnimator`, projectile/AoE replication path가 아직 없다.
* 따라서 client HP test를 진행하려면 먼저 `Host truth -> client display` 최소 mirror가 필요했다.

### 무엇을 수정했는가
* `MultiplayerPlayerAvatar`에 `BossPresentationSnapshot` struct와 owner-targeted `ClientRpc`를 추가했다.
* server authority replica는 network tick마다 boss transform + animator state를 sample해 owner client로 push한다.
* client owner는 local boss reference를 다시 찾고, disabled boss object에 transform/animator state를 apply해 movement와 current attack animation state를 보이게 했다.
* scene coordinator는 client local boss `CharacterController`도 꺼서 physics/transform 충돌 가능성을 줄였다.

### 왜 그렇게 판단했는가
* dedicated boss netcode actor를 지금 새로 올리면 scope가 커지고 verify unblock까지 오래 걸린다.
* existing owner-targeted player avatar RPC path를 reuse하면 new scene object networking 없이도 remote client display unblock이 가능하다.
* current user goal은 `client HP test 전에 boss가 움직이고 공격하는 모습이 보이는가`이므로, display-only mirror가 가장 작은 정답이다.

## Topic 3. multiplayer player HUD HP sync + viewer-side labels

## 오늘 반영한 작업
* `MultiplayerPlayerAvatar`에 server write 전용 `HUD current/max HP` 네트워크 스냅샷을 추가하고, local-owned avatar가 자기 화면의 main/partner HUD를 모두 다시 그리도록 연결했다.
* `PlayerController.InitializeCombatHUD()`는 solo에서는 기존처럼 local `Health`를 HUD source로 유지하고, active multiplayer session에서는 player source를 `null`로 두는 manual HUD mode로 전환해 stale local HP가 authoritative HUD를 덮어쓰지 못하게 정리했다.
* `CombatHUDController`는 `PartnerHUD_Panel` 내부의 `Image_PartnerHP_Fill` / `Text_PartnerName`를 runtime에서 찾아 partner HP/name setter를 제공하도록 확장했다.
* viewer-side naming rule을 적용해 Host screen은 `Host(me)` / `Client`, Client screen은 `Client(me)` / `Host`로 보이게 맞췄다.

## 체크리스트 업데이트
* [x] Host 화면에서 main HUD는 `Host(me)`, partner HUD는 `Client`를 표시한다.
* [x] Client 화면에서 main HUD는 `Client(me)`, partner HUD는 `Host`를 표시한다.
* [x] boss가 Host 또는 Client를 때릴 때 양쪽 화면에서 local/partner HP fill이 Host truth 기준으로 함께 갱신된다.
* [x] solo path는 기존 local `Health` 기반 HUD binding을 유지한다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user report 기준으로 verify multiplayer test에서는 client player HP truth는 줄어들었지만, Host/Client 양쪽 화면의 player HUD fill/name이 모두 stale 상태로 남아 있었다.
* `step 7` health gate 이후 client local `Health`는 더 이상 final truth가 아니므로, multiplayer HUD가 raw local `Health`를 직접 믿는 구조는 계속 틀릴 수밖에 없었다.
* scene UI 쪽에는 이미 `PartnerHUD_Panel`이 준비돼 있었기 때문에, 이번 slice는 prefab 재작업보다 code-side authoritative bind를 먼저 완성하는 쪽이 맞았다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* current HUD 구조는 player main slot만 event/local source 기반으로 갱신하고 있어, partner panel은 보이기만 하는 reserve 상태였다.
* multiplayer에서는 local client `Health` 값이 Host truth와 분리될 수 있으므로, HUD source를 `Health` 하나에만 묶어 두면 양쪽 화면 모두 쉽게 stale해진다.
* verify 요구사항은 `내 HP`와 `상대 HP`를 각 화면에서 동시에 보이게 하는 것이므로, viewer-side mapping이 필요했다.

### 무엇을 수정했는가
* `MultiplayerPlayerAvatar`가 server-authoritative HP를 HUD용 `NetworkVariable<int>` current/max pair로 내보내고, local owner가 자신의 `CombatHUDController`에 main/partner 값을 직접 적용하게 했다.
* `PlayerController.InitializeCombatHUD()`에서 multiplayer session이면 player `Health` source를 비워 HUD polling/event path를 끄고, avatar-driven manual update가 승자가 되도록 만들었다.
* `CombatHUDController`에 partner HP/name API와 runtime child lookup을 추가해 scene/prefab 수동 연결 없이도 `PartnerHUD_Panel`을 실제 데이터 슬롯으로 쓸 수 있게 했다.

### 왜 그렇게 판단했는가
* quick local-only patch를 하면 한 화면만 맞고 다른 화면 partner HP가 계속 틀릴 가능성이 높았다.
* HUD용 current/max snapshot을 avatar에 붙이면 Host/Client 모두 같은 source를 읽을 수 있어, 이후 reaction/applier 단계와도 자연스럽게 이어진다.
* viewer-relative label을 runtime에서 계산해야만 `Host(me)` / `Client(me)` 요구사항을 두 화면에서 동시에 만족할 수 있다.

## Topic 4. boss closest live player retarget for multiplayer verify

## 오늘 반영한 작업
* `BossController`에 현재 씬의 살아있는 `PlayerController` 중 가장 가까운 대상을 다시 고르는 `RefreshClosestLiveTarget(...)` helper를 추가했다.
* `BossIdleState`, `BossCombatState`, `BossSearchingState`, `BossHitState`는 non-attack state update 시 이 helper를 호출해, boss가 spawn 시점에 잡힌 한 명만 계속 바라보지 않도록 정리했다.
* current refresh는 `0.1s` 간격으로 throttled 되며, solo/multiplayer 공통으로 동작한다.

## 체크리스트 업데이트
* [x] boss가 verify multiplayer scene에서 client만 고정 추적하지 않고, 가장 가까운 살아있는 player를 다시 target으로 선택한다.
* [x] host player가 detection range에 먼저/더 가깝게 들어오면 boss가 host 쪽 combat/detection path로 진입할 수 있다.
* [x] solo path는 single player만 있으므로 기존 동작을 유지한다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user test 기준으로 current verify multiplayer run에서는 boss가 client player HP는 줄일 수 있었지만, host player가 detection circle에 들어와도 combat으로 진입하지 않았다.
* inspection 결과 boss target은 multiplayer spawn 직후 한 번만 rebind되고, 그 뒤 detection/combat/search는 그 stored target만 계속 읽고 있었다.
* current goal은 boss aggro system을 완성하는 것이 아니라 verify hit test unblock이므로, nearest live player retarget을 temporary rule로 채택했다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* `MultiplayerGameplaySceneCoordinator`의 boss target rebind는 authoritative player spawn 직후 1회만 실행되고 있었다.
* boss FSM의 detection/search/combat path는 `Controller.Target` 하나만 계속 읽으므로, 첫 target이 client이면 host가 더 가까워져도 감지 기준이 바뀌지 않았다.
* verify blocker는 `host도 boss에게 맞아야 HP test를 계속할 수 있다`였지, 완성형 aggro 우선순위가 아니었다.

### 무엇을 수정했는가
* `BossController`가 일정 간격으로 현재 씬의 살아있는 player 중 planar distance가 가장 짧은 대상을 다시 선택하게 했다.
* non-attack boss state들은 update 진입 시 이 retarget helper를 호출해, detection/combat/search decision이 latest closest player 기준으로 평가되게 했다.
* attack state 자체는 건드리지 않아 현재 공격 실행 계약은 유지했다.

### 왜 그렇게 판단했는가
* scene coordinator의 spawn-time one-shot rebind만으로는 runtime target 교체가 되지 않으므로, boss runtime 쪽에 작은 retarget 규칙이 필요했다.
* full aggro/contribution system을 지금 넣으면 scope가 커지므로, verify unblock에는 `closest live player` 규칙이 가장 작고 명확했다.
* attack 중 target까지 계속 흔들면 패턴 contract가 커질 수 있으므로, 이번 slice는 non-attack state refresh까지만 제한하는 편이 안전했다.

## Topic 5. player authority step 8 execute path

## 오늘 반영한 작업
* `PlayerController`에 `ActionAuthorityMode`를 추가해 `SoloLocal / HostAuthoritative / ClientOwnerProxy / RemoteDisplayOnly`를 구분하고, multiplayer에서는 local input이 `Attack1`을 직접 시작하지 못하게 정리했다.
* `MoveState`, `JumpState`, `AttackState`는 multiplayer에서 local `AttackState` direct transition, local combo reserve, local attack-to-dash cancel을 막고, solo path만 기존 local input 규칙을 유지하도록 정리했다.
* `MultiplayerPlayerAvatar`는 Host가 accept한 `Attack1`을 authoritative `PlayerController`에 즉시 실행하고, owner client에는 owner-targeted `ClientRpc`로 approved attack start와 reaction snapshot을 apply하게 연결했다.
* owner client apply는 `reaction sequence` dedupe와 accepted start tick 기반 elapsed-time 보정으로 duplicate replay와 late attack exit를 줄이고, client owner proxy는 local hitbox enable / local boss-hit reaction 대신 Host result만 따라가게 맞췄다.

## 체크리스트 업데이트
* [x] multiplayer에서 `Attack1`은 `Host approve -> PlayerController execute` path로만 시작된다.
* [x] old local `AttackState` direct transition이 `MoveState` / `JumpState`에서 multiplayer path로 새지 않게 막았다.
* [x] owner client는 owner-targeted apply path로 `Attack1 / hit / stun / death`를 1회만 반영한다.
* [x] client owner proxy는 local hitbox와 local boss-hit reaction을 직접 쓰지 않고 Host result snapshot만 따른다.
* [x] dash current authority path는 유지하고, 이번 slice의 main focus를 `Attack1 / hit / stun`으로 제한했다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user concern 기준 current risk는 `owner client attack start delay`, `old local attack transition double-start`, `reaction double-apply`, `solo/multiplayer path mix` 네 가지였다.
* current codebase에는 Host validator/state/reaction trace는 있었지만, `Host approve -> PlayerController execute`와 owner apply path가 분리돼 있지 않아 `step 8` verify가 어려웠다.
* 이번 slice 목표는 full `Replicator / Applier` cleanup이 아니라, first execution rule을 먼저 고정해 `Attack1 / hit / stun`의 authority boundary를 안전하게 만드는 것이었다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* `MoveState` / `JumpState`에는 multiplayer role과 상관없이 local attack transition이 남아 있어, validator path와 race가 날 수 있었다.
* owner client는 Host accept 후 `Attack1`을 apply하는 dedicated path가 없어서, later apply를 붙이면 duplicate start / duplicate reaction 위험이 컸다.
* client owner proxy가 local hitbox나 local boss-hit reaction을 직접 쓰면 Host truth와 다른 local-only gameplay branch가 생길 수 있었다.

### 무엇을 수정했는가
* `PlayerController`에 authority mode gate와 `TryStartAuthoritativeAttack1(...)`, `ApplyAuthoritativeReactionSnapshot(...)`를 추가했다.
* multiplayer state classes는 local `Attack1` start / combo chain / attack-dash cancel을 solo-only로 좁혔다.
* `MultiplayerPlayerAvatar`는 Host accept 시 authoritative controller를 직접 실행하고, owner-targeted `ClientRpc`로 approved attack start와 reaction snapshot을 내려 보내도록 연결했다.
* owner client apply는 action/reaction sequence dedupe를 두고, attack start는 accepted start tick 차이만큼 local timer를 앞당겨 late exit를 줄였다.

### 왜 그렇게 판단했는가
* `step 8`의 핵심은 `Host truth를 만들었다`가 아니라 `Host truth가 실제 player FSM execute까지 닿는다`는 점이므로, execution gate를 먼저 고정해야 했다.
* full remote display replication까지 한 번에 열면 scope가 커지므로, owner client runtime apply와 duplicate 방지부터 먼저 닫는 편이 안전했다.
* solo behavior를 깨지 않으려면 separate code path보다 `ActionAuthorityMode`로 role split을 명시하는 편이 기존 FSM과 multiplayer glue를 함께 지키기 쉬웠다.

## Topic 6. client owner attack facing direction fix

## 오늘 반영한 작업
* `PlayerController`에 `GetAttackFacingDirection()` helper를 추가해, client owner proxy는 attack start 시 stale `cameraRoot.forward` 대신 latest `lookYaw` 기반 전방을 우선 사용하도록 정리했다.
* `AttackState.RotateToCamera()`는 raw `CameraRoot.forward` 직접 참조를 제거하고, controller helper가 반환한 facing direction으로 회전하도록 교체했다.
* fallback rule은 `client owner latest look yaw -> cameraRoot.forward -> transform.forward` 순서로 두어 solo/host path는 기존 감각을 유지하고, build client attack facing만 좁게 수정했다.

## 체크리스트 업데이트
* [x] build client owner가 `Attack1` 시작 시 latest look yaw 방향으로 먼저 바라보도록 수정했다.
* [x] solo / host attack facing path는 기존 `cameraRoot` 기반 동작을 유지한다.
* [x] 이번 slice는 attack facing source만 조정하고 authority / dash path는 건드리지 않았다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user verify 결과 기준 build client에서는 attack start 시 player가 camera forward를 보지 않고, host screen과 다른 방향으로 attack하는 문제가 보였다.
* current multiplayer client owner proxy는 look-driven `cameraRoot` 갱신을 끈 상태라, `AttackState`가 `cameraRoot.forward`를 바로 읽으면 stale forward를 잡을 수 있었다.
* 이번 fix 목표는 `step 8` authority flow를 다시 열지 않고, attack facing source만 좁게 바로잡는 것이었다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* client owner proxy는 local input에서 `lookYaw`는 최신으로 들고 있지만, runtime role 설정상 `cameraRoot` 자체는 항상 same-frame forward source가 아니었다.
* `AttackState.RotateToCamera()`는 이 stale `cameraRoot.forward`를 그대로 써서 build client attack facing mismatch를 만들 수 있었다.
* host/solo에서는 current `cameraRoot` 기준이 이미 맞게 동작하므로, 전역 회전 규칙을 바꾸는 것은 과한 수정이었다.

### 무엇을 수정했는가
* `PlayerController.GetAttackFacingDirection()`를 추가해 attack facing source selection을 한 곳으로 모았다.
* client owner proxy는 latest `lookYaw`로 forward를 만들고, 나머지 role은 기존 `cameraRoot.forward`를 우선 사용한다.
* `AttackState.RotateToCamera()`는 이제 controller helper를 통해 계산된 방향으로 회전한다.

### 왜 그렇게 판단했는가
* facing mismatch의 원인은 attack authority가 아니라 direction source stale 문제라서, `AttackState` 전체 흐름보다 facing source만 바꾸는 편이 가장 안전했다.
* `cameraRoot` update rule을 multiplayer 전체에 다시 켜면 dash/camera/presentation path까지 넓게 흔들릴 수 있었다.
* helper method로 source selection을 모아 두면 이후 dash facing이나 other action facing issue도 같은 규칙으로 확장하기 쉽다.

## Topic 7. authoritative attack facing yaw sync

## 오늘 반영한 작업
* `ClientToHostPlayerActionIntent`에 `FacingYaw`를 추가해 attack edge가 발생한 순간의 yaw를 Host-approved action payload에 함께 실었다.
* `MultiplayerPlayerAvatar`는 local observe path와 server-buffer path 모두에서 같은 `FacingYaw`를 기록하고, Host accept 시 그 yaw로 authoritative `Attack1` start를 실행하며 owner client apply에도 같은 yaw를 다시 사용하도록 연결했다.
* `PlayerController`는 pending authoritative attack facing yaw를 한 번 소비하는 경로를 추가해, attack start 회전이 `later local cache`가 아니라 `accepted attack yaw`를 기준으로 일어나게 맞췄다.

## 체크리스트 업데이트
* [x] multiplayer `Attack1` start는 local current yaw 추정이 아니라 action-intent에 실린 accepted `FacingYaw`를 사용한다.
* [x] Host screen의 remote client player attack facing과 build client own-view attack facing이 같은 yaw source를 공유한다.
* [x] 이번 slice는 `Attack1` facing only sync이며 dash/locomotion facing rule은 변경하지 않았다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user verify 기준 first `Attack1`은 반쯤만 돌고, second `Attack1`은 fully forward를 보는 현상이 남아 있었다.
* 이 패턴은 generic connection failure보다 `attack start 순간 어떤 yaw를 읽었는가`가 frame/timing에 따라 달라지는 문제에 더 가까웠다.
* 따라서 최종 목표는 local fallback 보강이 아니라, `captured yaw -> Host accept -> owner apply` 전부가 같은 yaw를 쓰는 deterministic path를 만드는 것이었다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* client owner는 first attack start에서 local latest yaw / cameraRoot / Host-approved start timing이 완전히 묶여 있지 않아, first turn만 half-facing처럼 보일 수 있었다.
* disconnect log는 much later에 찍혀 있어, 이번 symptom은 generic network drop보다 attack-facing source mismatch 가능성이 더 높았다.
* local fallback만으로는 `first attack uses old yaw, second attack uses settled yaw` 같은 timing artifact를 완전히 없애기 어려웠다.

### 무엇을 수정했는가
* action-intent payload에 `FacingYaw`를 추가했다.
* local observe, server-buffer observe, Host validate/execute, owner apply trace가 모두 same `FacingYaw`를 들고 가도록 맞췄다.
* `PlayerController`는 attack start 직전에 pending authoritative yaw를 소비해 `AttackState.RotateToCamera()`가 accepted yaw를 먼저 사용하게 만들었다.

### 왜 그렇게 판단했는가
* attack facing은 “현재 프레임에 읽힌 local yaw”보다 “Host가 승인한 action의 yaw”가 더 안정적인 truth다.
* same accepted yaw를 Host와 owner client가 함께 쓰면 first attack/second attack 차이를 만드는 frame-order noise를 줄일 수 있다.
* payload 확장 비용은 작고 scope도 `Attack1` start에만 묶을 수 있어, wider camera/dash path를 흔드는 것보다 안전하다.

## Topic 8. client dash exit walk-without-input fix

## 오늘 반영한 작업
* `PlayerLocomotionCore`에서 dash가 끝나는 same tick에 move input이 0이면 residual dash planar velocity를 `Vector3.zero`로 정리하도록 추가했다.
* dash 종료 animator handoff는 이제 `wasDashActive && !nextDashActive`를 `dashEndedThisTick`으로 명시해, no-input exit tick에서 `Speed`가 walk처럼 올라가지 않게 맞췄다.
* 이 수정은 dash motion 자체를 바꾸지 않고, dash 종료 직후 locomotion blend source만 좁게 정리하는 범위로 유지했다.

## 체크리스트 업데이트
* [x] build client dash 종료 직전에 input 없이 walk blend가 잠깐 뜨는 symptom을 막는 no-input exit guard를 추가했다.
* [x] same fix가 authoritative state에도 반영되어 owner apply/replay가 같은 residual speed를 다시 살리지 않도록 맞췄다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user verify 기준 client dash 끝무렵에 input을 누르지 않아도 player가 한 번 walk처럼 보이고 나서 idle로 떨어졌다.
* current shared locomotion core는 마지막 dash 이동량으로 `nextPlanarVelocity`를 계산한 뒤, same tick에 dash가 끝나면 그 속도를 locomotion animator `Speed`로 넘기고 있었다.
* Host 화면보다 client owner에서 더 잘 보인 이유는 predicted/apply path가 이 shared `PlanarVelocity`를 그대로 animator magnitude source로 다시 사용하기 때문이었다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* dash 마지막 tick에서는 실제 위치 이동이 여전히 크기 때문에 `nextPlanarVelocity`가 높게 남는다.
* 그 직후 `nextDashActive == false`가 되면 code는 `Locomotion`으로 crossfade하면서도, 방금 계산한 높은 planar speed를 `Speed` parameter에 그대로 넣고 있었다.
* 이 값은 state에도 저장되므로 owner authoritative apply/replay도 같은 tick에서 phantom walk를 다시 재생할 수 있었다.

### 무엇을 수정했는가
* `dashEndedThisTick` helper bool을 추가해 dash exit handoff 시점을 명시했다.
* `dashEndedThisTick && input.moveDir == 0` 조건에서는 `nextPlanarVelocity`를 0으로 재설정했다.
* 이후 locomotion blend speed 계산과 animator handoff가 이 zeroed velocity를 사용하게 맞췄다.

### 왜 그렇게 판단했는가
* 문제의 핵심은 물리 이동이 아니라 animator blend source라서, dash duration이나 cooldown rule을 건드릴 필요가 없었다.
* `PlanarVelocity`는 current code에서 locomotion visual magnitude source로만 쓰이므로, no-input dash exit tick에 한해 0으로 정리해도 gameplay truth를 넓게 흔들지 않는다.
* state 자체를 clean하게 저장하면 local prediction, Host authoritative state, owner replay가 모두 같은 fix를 공유할 수 있다.

## Topic 9. client dash visual smoothness fix

## 오늘 반영한 작업
* `MultiplayerPlayerPresentationDriver`가 dash active 전체 구간에서 predicted presentation을 강제 snap하지 않도록 수정했다.
* presentation driver는 이제 `dash started this frame`일 때만 one-shot snap을 쓰고, dash travel frame들은 기존 interpolation path로 부드럽게 따라가도록 바꿨다.
* reset path에서는 dash presentation transition cache도 함께 초기화해 mode switch나 local presentation disable 뒤에 stale dash-start 판정이 남지 않게 정리했다.

## 체크리스트 업데이트
* [x] build client dash visual이 whole dash 동안 tick-step처럼 끊겨 보이던 forced snap rule을 제거했다.
* [x] dash start responsiveness는 유지하고, dash travel은 predicted render smoothing을 다시 사용한다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `System_Blueprint`, `Technical_Glossary`를 동기화했다.

## 맥락노트
* user verify 기준 Host dash는 자연스러웠지만 build client dash는 laggy/choppy하게 보였다.
* current presentation driver는 `_controller.IsDashStateActive`가 true인 whole dash duration 동안 predicted transition을 계속 snap하고 있었다.
* 이 규칙은 dash start 첫 순간에는 반응성을 주지만, 전체 dash에 적용되면 render proxy가 tick target로 계단식 이동을 반복하게 만들어 visual smoothness를 떨어뜨렸다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* `UpdatePredictedLocomotionPresentation(...)`는 `IsDashStateActive` 하나만으로 snap 여부를 결정해, dash 중에는 every frame smoothing을 우회하고 있었다.
* snap path는 presentation position을 target으로 바로 재설정하므로, owner build 화면에서는 dash가 smooth slide보다 작은 step 이동처럼 보일 수 있었다.
* dash gameplay truth나 authority path보다, local render proxy choice가 choppy 느낌의 직접 원인에 더 가까웠다.

### 무엇을 수정했는가
* presentation driver에 previous dash flag를 추가했다.
* 현재 frame이 `dash started`인지 판단해, 이 시점에만 snap을 허용한다.
* dash continue frame은 existing interpolation path를 그대로 사용하도록 변경했다.

### 왜 그렇게 판단했는가
* dash start만 snap하면 첫 반응은 빠르게 유지하면서도, travel 구간에서는 smooth render proxy 이점을 되찾을 수 있다.
* gameplay sim / authority / cooldown을 건드리지 않고도 visual symptom을 줄일 수 있어 scope가 가장 작다.
* dash visual bug의 성격이 render smoothing policy라서, presentation driver 한 파일에서 끝내는 편이 가장 안전했다.

## Topic 10. player authority step 9 send boundary freeze document sync

## 오늘 반영한 작업
* `docs/technical/multiplayer/Multiplayer_Player_Action_Authority.md`의 `step 9`를 current verify result 기준 `[x]`로 올리고, `MultiplayerPlayerAvatar` hybrid send path boundary를 표로 고정했다.
* current send path를 `owner locomotion truth`, `owner approved action start`, `owner reaction snapshot`, `HUD HP NetworkVariable`, `viewer-side label`, `remote display-only sync` 여섯 갈래로 나눠 later `Replicator` target scope를 정리했다.
* user-confirmed manual verify pass를 문서에 반영해, current `step 9` 목표가 new class 구현이 아니라 `one packet / one path` cleanup boundary freeze임을 명시했다.

## 체크리스트 업데이트
* [x] current temporary send path와 later `HostToClientPlayerStateReplicator` target scope를 분리 문서화했다.
* [x] Host/Client 양쪽 화면 verify result를 authority 문서의 current boundary read에 반영했다.
* [x] `Progress_Log`, `README`, `System_Blueprint`, `Technical_Glossary`, authority 문서를 같은 boundary wording으로 동기화했다.

## 맥락노트
* `step 8`까지는 Host approve/execute와 owner apply가 붙었지만, send-side responsibility는 아직 `MultiplayerPlayerAvatar` 안에 mixed 형태로 남아 있었다.
* current verify pass에서는 new `Replicator` class를 서둘러 추가하는 것보다, 어떤 값이 어떤 temporary path로 가는지 먼저 고정하는 편이 later cleanup risk를 줄인다.
* user가 Host/Client manual test를 이미 끝낸 상태였기 때문에, 이번 slice는 implementation보다 `verified boundary document`를 남기는 것이 핵심이었다.

## 코드 검사 결과 (코드 변경 없음)
* 명령: 미실행
* 결과: 문서 동기화 only
* 미실행 사유 및 대체 검증: 이번 topic은 문서화만 수행했다. code-side build 결과는 same-day earlier topics에서 이미 기록했고, runtime verify는 user manual Host/Client test pass를 기준으로 반영했다.

## 기술적 고려
### 무엇을 발견했는가
* current multiplayer player path는 single `Replicator`가 아니라 `RPC + NetworkVariable + local derived label + display sync`가 섞인 hybrid send path였다.
* 이 상태를 문서 없이 다음 step으로 넘기면 `what is gameplay truth`와 `what is display-only`가 다시 섞일 위험이 있었다.
* remote partner `dash / Attack1 / hit / stun / death`는 current runtime에서 dedicated state packet보다 `NetworkTransform / NetworkAnimator` display sync 해석이 더 맞았다.

### 무엇을 수정했는가
* authority 문서에 current send path map과 later `Replicator` target scope table을 추가했다.
* same verified read를 `Progress_Log`, `README`, `System_Blueprint`, `Technical_Glossary`에도 함께 반영했다.
* Host/Client 두 화면 verify result를 `current HP / partner HP / label / owner action-reaction / remote display-only` 기준으로 문장화했다.

### 왜 그렇게 판단했는가
* `step 9`의 value는 new transport class 추가보다 `current temporary answer를 정확히 freeze`하는 데 있다.
* send boundary를 먼저 고정하면 `step 10-11`에서 apply/display cleanup을 하더라도 기존 verified contract를 잃지 않는다.
* user가 이미 runtime verification을 마친 상태이므로, 지금 필요한 것은 new implementation이 아니라 later refactor 기준점을 남기는 문서다.

## Topic 11. player authority step 10 apply boundary freeze document sync

## 오늘 반영한 작업
* `docs/technical/multiplayer/Multiplayer_Player_Action_Authority.md`의 `step 10`을 current verify result 기준 `[x]`로 올리고, current mixed apply path를 `owner runtime apply / owner HUD apply / remote display-only` 세 갈래로 정리했다.
* owner apply entry를 `locomotion truth`, `approved Attack1 start`, `reaction snapshot`, `HUD mapper`로 나누고, remote는 `SimulationMode.Disabled + RemoteDisplayOnly + NetworkTransform/NetworkAnimator` display sync 해석으로 고정했다.
* later `HostToClientPlayerStateApplier`가 가져갈 범위와 지금 밖에 두어야 할 범위를 분리 문서화했다.

## 체크리스트 업데이트
* [x] current mixed apply path의 owner/runtime/HUD/display 역할을 분리 문서화했다.
* [x] owner screen이 local-only result를 확정하지 않는 rule과 remote screen이 partner gameplay runtime을 직접 돌리지 않는 rule을 authority 문서에 고정했다.
* [x] `Progress_Log`, `README`, `System_Blueprint`, `Technical_Glossary`, authority 문서를 같은 apply-boundary wording으로 동기화했다.

## 맥락노트
* `step 9`가 send-side boundary freeze였다면, `step 10`은 apply-side boundary freeze다.
* current runtime은 send도 apply도 `MultiplayerPlayerAvatar` 안에 mixed 형태로 남아 있기 때문에, later cleanup 전에는 `who applies what`를 먼저 고정해야 한다.
* user가 같은 Host/Client verify run을 이미 끝낸 상태였으므로, 이번 slice는 새 runtime class 추가보다 `current owner/remote split`를 문서에 남기는 것이 핵심이었다.

## 코드 검사 결과 (코드 변경 없음)
* 명령: 미실행
* 결과: 문서 동기화 only
* 미실행 사유 및 대체 검증: 이번 topic은 문서화만 수행했다. code-side build 결과는 same-day earlier topics에서 이미 기록했고, runtime verify는 user manual Host/Client test read를 기준으로 반영했다.

## 기술적 고려
### 무엇을 발견했는가
* current client-side apply는 dedicated `Applier` class가 아니라 owner runtime apply, owner HUD apply, remote display-only sync가 섞인 구조였다.
* send boundary를 고정해도 apply boundary를 문서화하지 않으면 later cleanup에서 remote gameplay runtime이 다시 열리거나, owner local-only confirm이 다시 섞일 위험이 있었다.
* current remote partner `dash / Attack1 / hit / stun / death` visible result는 gameplay FSM apply보다 `NetworkTransform / NetworkAnimator` display sync interpretation이 더 맞았다.

### 무엇을 수정했는가
* authority 문서에 current mixed apply path map과 later `HostToClientPlayerStateApplier` target scope table을 추가했다.
* owner client runtime apply, owner HUD apply, remote display-only rule을 `Progress_Log`, `System_Blueprint`, `Technical_Glossary`에도 같은 wording으로 반영했다.
* test appendix numbering도 `11.3`으로 내려, `#9 send boundary`와 `#10 apply boundary`를 각각 독립 섹션으로 읽게 정리했다.

### 왜 그렇게 판단했는가
* later `Applier` cleanup의 가장 큰 리스크는 `owner runtime truth`와 `remote display-only`를 다시 섞는 것이다.
* apply-side boundary를 먼저 고정하면 code extraction을 하더라도 current verified screen behavior를 유지하기 쉬워진다.
* current task의 정답은 새 class 구현보다 `owner applies Host result / remote only displays partner`라는 contract를 명확히 남기는 것이다.

## Topic 12. player authority step 11 presentation driver visual-only freeze

## 오늘 반영한 작업
* `MultiplayerPlayerPresentationDriver` summary와 gating rule을 정리해, 이 클래스가 `gameplay truth / FSM authority / HP-result apply`가 아니라 `owner visual polish`만 담당한다는 contract를 코드와 문서 양쪽에 고정했다.
* driver는 이제 `PredictedLocomotion + ClientOwnerProxy + local presentation enabled` 조건에서만 predicted visual smoothing path를 사용하도록 더 좁혔다.
* 사용되지 않는 presentation velocity cache를 제거해, driver가 실제로 다루는 값이 visual child transform과 camera follow surface뿐임을 더 분명하게 만들었다.

## 체크리스트 업데이트
* [x] presentation driver를 owner visual-only path로 더 좁혔다.
* [x] `Attack1` 시작/피격 반응/HP truth apply는 driver 책임이 아님을 authority 문서에 고정했다.
* [x] `dotnet build BossRaidPortfolio.sln`로 코드 검사를 통과했다.
* [x] `Progress_Log`, `README`, `System_Blueprint`, `Technical_Glossary`, authority 문서를 같은 visual-only wording으로 동기화했다.

## 맥락노트
* `step 9-10`에서 send/apply boundary를 고정한 뒤에도, presentation driver가 owner visual layer 이상을 맡는 것처럼 읽히면 later cleanup에서 hidden authority path로 오해될 수 있었다.
* current code를 읽으면 driver는 이미 mostly visual-only였지만, gate가 `PredictedLocomotion` 중심이라 role intent가 코드에 충분히 드러나지 않았다.
* 이번 slice 목표는 bigger refactor가 아니라, existing verified dash smoothing path를 유지하면서 visual-only contract를 더 명시적으로 만드는 것이었다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* current `MultiplayerPlayerPresentationDriver`는 actual behavior상 visual-only에 가까웠지만, code contract 차원에서는 `ClientOwnerProxy` owner path 한정이라는 의도가 충분히 드러나지 않았다.
* driver가 dash state와 move input을 읽고 있기 때문에, later reader가 잘못 보면 hidden gameplay authority처럼 오해할 수 있었다.
* `_presentationWorldVelocity` cache는 current code에서 읽히지 않아, visual-only boundary를 설명할 때 오히려 noise가 됐다.

### 무엇을 수정했는가
* class summary를 visual-only responsibility 중심으로 다시 적었다.
* predicted presentation path gate를 `PredictedLocomotion + ClientOwnerProxy + local presentation enabled` 조합으로 좁혔다.
* 사용되지 않는 `_presentationWorldVelocity` field와 관련 write를 제거했다.
* authority 문서에 step 11 visual-only boundary freeze 섹션을 추가했다.

### 왜 그렇게 판단했는가
* visual helper가 어떤 role에서만 살아야 하는지 코드에 직접 적어 두면 later cleanup에서 hidden authority path를 다시 만들 가능성이 줄어든다.
* dash local feel은 유지해야 하지만, 그 이유가 `presentation timing polish`임을 문서와 코드에서 함께 보여 주는 편이 안전하다.
* 사용되지 않는 cache를 남겨 두는 것보다 제거하는 편이 class responsibility를 더 빠르게 읽게 해 준다.

## Topic 13. multiplayer combo chain + attack-cancel dash authority follow-up

## 오늘 반영한 작업
* multiplayer combo chain이 `Attack1 only`에 묶여 끊기던 문제를 `Host approve -> PlayerController execute` follow-up으로 확장해, `Attack2 / Attack3`도 owner local direct confirm 없이 same authority path로 시작되게 정리했다.
* attack 중 dash input이 multiplayer에서 solo-only gate와 Host reject에 막히던 문제를 고쳐, valid cancel window에서는 Host가 `AttackState -> DashState`를 authoritative하게 열고 owner가 approved dash start를 따라가게 만들었다.
* current authority/boundary 문서도 same-day runtime read에 맞게 다시 동기화해, `Attack1 only`와 `attack-to-dash cancel solo path`를 initial slice note로만 읽게 정리했다.

## 체크리스트 업데이트
* [x] combo continuation은 Host combo window validate 뒤에만 `Attack2 / Attack3`로 이어진다.
* [x] attack-cancel dash는 valid cancel window에서만 Host approve 후 시작된다.
* [x] owner client는 local-only combo continue / dash-cancel result를 확정하지 않는다.
* [x] `dotnet build BossRaidPortfolio.sln` 기준 code/build consistency를 유지했다.
* [x] `Progress_Log`, `README`, `System_Blueprint`, `Technical_Glossary`, authority 문서를 current runtime wording으로 동기화했다.

## 맥락노트
* `step 8~11` 문서는 initial slice 기준 `Attack1 only`, `attack-to-dash cancel solo path` wording을 일부러 남기고 있었지만, same-day bug fix가 들어오면서 current runtime read와 어긋나기 시작했다.
* combo와 dash cancel 둘 다 공통 문제는 `owner local direct confirm`을 다시 열지 않고, Host-approved action start 범위를 넓히는 쪽으로 해결해야 한다는 점이었다.
* 그래서 이번 문서 sync는 old step history를 지우기보다, `initial slice`와 `current runtime follow-up`을 함께 읽히게 정리하는 쪽을 택했다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build BossRaidPortfolio.sln`
* 결과: 성공 (Warning 다수, Error 0)
* 미실행 사유 및 대체 검증: Unity Host/Client manual verify는 user report 기준 `combo chain`과 `attack-cancel dash`가 정상 동작하는 것으로 확인했다.

## 기술적 고려
### 무엇을 발견했는가
* current doc set은 `step 8` initial slice 설명이 강해서, runtime이 이미 지원하는 `Attack2 / Attack3` combo continuation과 `attack-cancel dash` follow-up을 놓치고 있었다.
* send/apply boundary 문서도 owner approved action start를 `Attack1 only`로 적고 있어, current RPC/apply contract와 어긋났다.
* `System_Blueprint`의 `Attack Logic` row도 `local combo reserve와 attack-to-dash cancel은 solo path`라고 읽혀 current behavior를 잘못 설명하고 있었다.

### 무엇을 수정했는가
* authority 문서에 same-day follow-up note를 넣고, owner approved action start/send/apply 범위를 `Attack1 / Attack2 / Attack3 + attack-cancel dash`로 다시 적었다.
* `System_Blueprint`의 `Attack Logic`, `Player Action Authority Execute Slice`, `Send Boundary Freeze`, `Apply Boundary Freeze` wording을 current runtime 기준으로 고쳤다.
* `Technical_Glossary`에는 separate action-intent path, `ActionAuthorityMode`, combo/dash-cancel follow-up terms를 current behavior에 맞게 정리했다.

### 왜 그렇게 판단했는가
* combo와 dash cancel은 `new step`보다 same authority contract extension으로 읽는 편이 current code 구조와 더 맞다.
* owner local feel을 위해 local-only confirm을 다시 열면 earlier authority cleanup과 충돌하므로, 문서도 `Host approve first` contract를 중심으로 적는 편이 안전하다.
* later cleanup에서 `Replicator / Applier / PresentationDriver` boundary를 유지하려면, current action-start 범위를 정확히 남겨 두는 것이 중요하다.
