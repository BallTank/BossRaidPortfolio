# 2026-04-28 (Topic: Multiplayer Verify avatar split + TitleScene start bug fix)

## 오늘 반영한 작업
* `Assets/Resources/MultiplayerRuntimeConfig.asset` 기준으로 Host/Client avatar prefab을 분리했다.
* Host는 `Assets/Prefabs/Player/SoloPlayerAvatar.prefab`, Client는 `Assets/Prefabs/Player/MultiPlayerAvatar.prefab`을 사용하도록 runtime spawn path를 정리했다.
* `Assets/Scenes/mutiplayer/GamePlayScene_Verify.unity`에서 bootstrap `Player`를 비활성화하고, exact prefab template(`SoloPlayerAvatar`, `MultiPlayerAvatar`)를 scene 기준으로 유지했다.
* `Assets/Scripts/Multiplayer/UI/MultiplayerTitleSceneDriver.cs`에서 버튼 바인딩 대상을 whole-scene first-hit에서 runtime root subtree(`TitleRuntimeRoot (1)` 우선, 없으면 `TitleRuntimeRoot`)로 제한했다.
* `Assets/Scripts/Common/TitleSceneController.cs`에서 Play Mode multiplayer title scene일 때 `MultiplayerTitleSceneDriver` 누락을 자동 복구(self-heal attach)하도록 보강했다.

## 체크리스트 업데이트
* [x] Client avatar가 `MultiPlayerAvatar.prefab`(2P_3 visual)로 spawn되도록 적용
* [x] Host avatar가 `SoloPlayerAvatar.prefab`로 spawn되도록 적용
* [x] multiplayer title target scene이 `Assets/Scenes/mutiplayer/GamePlayScene_Verify.unity` 경로를 유지하도록 확인
* [x] duplicate button name 환경에서 Start/Join wrong-bind 방지 로직 적용
* [ ] Unity Play Mode Host/Client 동시 실기 검증 (`Tools/Build/FastMultiplayer Verify build`)

## 맥락노트
* Easy flow (avatar): `MultiplayerRuntimeConfig -> MultiplayerRuntimeRoot prefab resolve -> MultiplayerGameplaySceneCoordinator clientId-based spawn`.
* Easy flow (title start): `TitleSceneController.Awake -> EnsureMultiplayerDriverBound -> MultiplayerTitleSceneDriver.Start -> root-scoped button bind -> MultiplayerSessionService.StartGameplayAsync`.
* Root cause hypothesis for reported bug: duplicate `StartButton/JoinButton` names + missing driver wiring could route click events to wrong object/path, causing client entry failure and host-side start freeze.

## 코드 검사 결과 (코드 변경 작업 필수)
* 명령: `dotnet build BossRaidPortfolio.sln -v:minimal`
* 결과: 성공 (Error 0)
* 비고: 기존 프로젝트 warning(MSB3277/CS0414/CS0649)은 유지, 신규 error 없음

## 기술적 고려
### 무엇을 발견했는가
* `TitleScene.unity`에는 duplicate runtime roots와 duplicate button names가 공존한다.
* whole-scene name scan 방식 버튼 바인딩은 hidden/legacy button 오바인딩 위험이 있다.
* multiplayer title scene에서 driver 컴포넌트 누락 시 prototype path가 먼저 잡힐 수 있다.

### 무엇을 수정했는가
* Title driver 누락을 runtime self-heal로 복구했다.
* Title button binding scope를 preferred runtime root subtree로 제한했다.
* Host/Client avatar prefab split runtime path를 유지하고 verify scene 기준 template contract를 맞췄다.

### 왜 그렇게 판단했는가
* scene merge/누락 상황에서도 multiplayer flow를 안정적으로 유지하려면 self-heal attach가 안전하다.
* duplicate name 환경에서는 root-scoped binding이 의도한 UI contract와 가장 일치한다.
* verify scene 기반 빠른 빌드/테스트 목적에 맞게 gameplay start path를 고정 유지해야 회귀 검증 속도가 유지된다.

## Topic: GamePlayScene_Verify no-camera freeze fix (2026-04-28 follow-up)
### 오늘 반영한 작업
* `Assets/Scenes/mutiplayer/GamePlayScene_Verify.unity`에서 `CameraRoot`를 disabled legacy `Player` 자식에서 scene root로 분리했다.
* 같은 scene의 `SceneRoots`에 `CameraRoot` root transform을 추가해 root camera hierarchy가 안정적으로 유지되도록 정리했다.

### 체크리스트 업데이트
* [x] multiplayer verify scene에서 `Display 1 No cameras rendering` 재발 조건(카메라가 비활성 Player 자식) 제거
* [ ] Host/Client 동시 Play Mode에서 world render + 카메라 추적 수동 확인

### 맥락노트
* Cause: legacy `Player`가 비활성화되면 그 자식 `CameraRoot/Main Camera`도 함께 비활성화되어 UI만 보였다.
* Easy flow after fix: `CameraRoot (scene root, active) -> Main Camera active -> ThirdPersonCameraController가 local avatar followTarget을 runtime 재바인딩`.

### 기술적 고려
#### 무엇을 발견했는가
* `GamePlayScene_Verify`에서 `Main Camera`가 `CameraRoot`를 통해 disabled `Player` 계층 아래에 있었다.

#### 무엇을 수정했는가
* `CameraRoot` parent를 root로 변경하고 root list를 동기화했다.

#### 왜 그렇게 판단했는가
* legacy bootstrap player를 안전하게 비활성 유지하면서도 카메라 생명주기를 분리해야 multiplayer spawn/runtime 카메라가 안정적으로 동작한다.
